# Autonomous Dev Report — Couche 3 : évolution darwinienne de schémas de résolution
**Date** : 2026-06-13 07:42
**Project** : /mnt/projects/claude-arcade
**Stack** : TypeScript/Bun (web: React + Tailwind v4 + Framer Motion, react-force-graph-2d)
**Brief** : Étendre la consolidation pour extraire, par session, le sujet, la difficulté (+pourquoi), la liste exhaustive des problèmes et — par problème — son schéma de résolution. Regrouper les schémas par catégorie, élire un champion par fitness composite auto, conserver la lignée. Mesurer l'évolution dans le temps (recurrence rate ↓, fitness champion ↑). Injecter le champion pertinent dans le contexte (hooks SessionStart + UserPromptSubmit). Tout rendre visible dans l'app (5 onglets).

**Décisions actées (brainstorm Chris)** :
- Fitness = composite auto calculé : `w1·(1/turns)+w2·(1/backtracks+1)+w3·(1/tool_errors+1)+w4·quality_score`. Zéro jugement subjectif.
- Injection = SessionStart (champions du projet/cwd) + UserPromptSubmit (classifie chaque message → champion).
- Tout visible dans l'app.
- Métrique maîtresse d'auto-validation : taux de réapparition d'un problème de catégorie déjà vue (doit descendre) + fitness champion par catégorie dans le temps (doit monter).

---

## Solo Brainstorm — angles non couverts par le brainstorm joint

**Faisabilité de l'extraction (Phase A)**
- Le digest (`transcript-digest.ts`) préserve la séquence des tours, donc l'extraction des problèmes et schémas est faisable. MAIS budget 9000 chars head+tail → milieu tronqué sur sessions longues. **Décision** : augmenter le budget pour le pipeline enrichi (les problèmes peuvent vivre au milieu). Risque résiduel : sessions très longues restent partielles → acceptable v1.
- `turns_to_resolve / backtracks / tool_errors` par problème = **estimations LLM** (le LLM lit la séquence), pas des comptages déterministes. Suffisant pour amorcer la sélection. Durcissement possible plus tard (mesure transcript). Noté.

**Edge cases**
- Session sans problème (tout fluide) → `problems: []`, difficulty `easy`. Fitness N/A.
- Problème non résolu (`outcome: unresolved`) → exclu de l'élection de champion (un schéma qui n'a pas marché n'est pas un champion) mais conservé pour le recurrence tracking.
- Catégorie de problème vue une seule fois → champion trivial (lui-même), pas de comparaison. Devient signifiant à 2+.
- Schéma v1 (sessions déjà consolidées sans `problems`) → rétro-compat : champs absents = `[]`/defaults. Bump `SUMMARY_SCHEMA_VERSION` 1→2 ; les anciens résumés restent lisibles, re-consolidables à la demande.

**Dépendances implicites**
- Nouveaux fichiers d'état : `champions.json`, `evolution.json`, `injections.json` (dans `stateDir()/`).
- Hooks Claude Code (`SessionStart`, `UserPromptSubmit`) → config `~/.claude/settings.json` (hors repo) + scripts dans le repo. **Anti-récursion** : respecter `ARCADE_LOOP_ACTIVE=1` (déjà en place) pour ne pas s'auto-déclencher pendant la conso.
- Coût tokens : ré-consolidation enrichie des sessions = dépense cloud Sonnet. Le backfill complet reste gaté (déclenchement manuel).

**Risques d'intégration**
- `SummaryFields`/`SessionSummary` sont partagés (parse, store, insights, graph, api, web). Changement de type = ripple. → Story de types partagés en premier, additive uniquement (champs optionnels au narrowing).
- `rebuildInsights()` dans `run.ts` est le point de reconstruction déterministe → y brancher champions + evolution (cheap, zéro LLM).

**Contraintes d'ordre**
- A.1 (types) avant tout. Puis prompt/parse/digest en parallèle. Puis tests.
- B (champions) dépend de A. C (evolution) dépend de B. D (push) dépend de B. E (app) dépend de B/C/D.

---

## Plan

### Epic A — Socle data enrichi (extraction problèmes + schémas)
- Story A.1 — Étendre les types partagés : `subject`, `category`, `difficulty {level, why}`, `problems[]` (+ `resolution_schema`, `severity`, `outcome`). Bump SCHEMA_VERSION→2. | Files: src/consolidate/types.ts | Done when: tsc compile, champs additifs optionnels
- Story A.2 — Réécrire le prompt d'extraction (schéma enrichi, framework TIDD-EC appliqué). | Files: src/consolidate/summary-prompt.ts | Done when: prompt produit le JSON v2, exemple à jour [REQUIRES A.1]
- Story A.3 — Étendre `validateSummary` : narrowing strict des nouveaux champs + defaults sûrs. | Files: src/consolidate/parse.ts | Done when: objets v2 validés, v1 rétro-compat [REQUIRES A.1]
- Story A.4 — Augmenter le budget digest pour le pipeline enrichi. | Files: src/consolidate/transcript-digest.ts | Done when: milieu mieux préservé, coût borné [REQUIRES A.1]
- Story A.5 — Mettre à jour les tests parse pour le schéma v2. | Files: tests/parse.test.ts | Done when: bun test vert [REQUIRES A.1, A.3]

### Epic B — Catégorisation + fitness + champions
- Story B.1 — `fitness.ts` : composite pur testable. | Files: src/consolidate/fitness.ts
- Story B.2 — `champions.ts` : regroupement par catégorie, élection champion, lignée (champion_history). | Files: src/consolidate/champions.ts, types.ts [REQUIRES B.1]
- Story B.3 — Brancher champions dans `rebuildInsights` + persistance. | Files: src/consolidate/run.ts, store.ts [REQUIRES B.2]
- Story B.4 — Endpoints /api/champions, /api/champions/:category, /api/problems. | Files: src/server/api.ts [REQUIRES B.3]
- Story B.5 — Tests fitness + champions. | Files: tests/champions.test.ts [REQUIRES B.2]

### Epic C — Évolution / méta-mesure
- Story C.1 — `evolution.ts` : séries temporelles fitness champion, recurrence_rate, tendances. | Files: src/consolidate/evolution.ts, types.ts
- Story C.2 — Persistance + rebuild + endpoint /api/evolution. | Files: store.ts, run.ts, api.ts [REQUIRES C.1]
- Story C.3 — Tests evolution. | Files: tests/evolution.test.ts [REQUIRES C.1]

### Epic D — PUSH (injection dynamique)
- Story D.1 — `champion-context.ts` : champion(s) → texte injectable borné. | Files: src/consolidate/champion-context.ts
- Story D.2 — `classifier.ts` : texte → catégorie (réutilise text-normalize). | Files: src/consolidate/classifier.ts
- Story D.3 — Hook SessionStart : injecte champions du cwd. | Files: src/hooks/session-start.ts [REQUIRES D.1]
- Story D.4 — Hook UserPromptSubmit : classifie le prompt → injecte champion. | Files: src/hooks/user-prompt-submit.ts [REQUIRES D.1, D.2]
- Story D.5 — Trace des injections (persistance + endpoint /api/injections). | Files: store.ts, api.ts

### Epic E — App 5 onglets (tout visible)
- Story E.1 — Onglet Sessions enrichi (difficulté+pourquoi, problèmes). | Files: web/components/*
- Story E.2 — Onglet Problèmes (catégorie/sévérité/fréquence). | Files: web/components/ProblemsPanel.tsx
- Story E.3 — Onglet Schémas (comparaison + champion + breakdown fitness). | Files: web/components/SchemasPanel.tsx
- Story E.4 — Onglet Évolution (courbes). | Files: web/components/EvolutionPanel.tsx
- Story E.5 — Onglet Injection (trace) + navigation Sidebar. | Files: web/components/InjectionsPanel.tsx, Sidebar.tsx

---

## Execution

### Epic A — COMPLETE ✅
| Story | Status | Files | Integrity |
|---|---|---|---|
| A.1+A.2+A.3 (schéma trinity) | ✅ | types.ts, summary-prompt.ts, parse.ts | tsc 0 |
| A.4 (budget digest) | ✅ | transcript-digest.ts (9000→16000, 600→800, 50/50) | tsc 0 |
| A.5 (tests v2) | ✅ | tests/parse.test.ts, tests/insights.test.ts | bun test 23/23 |

- `difficulty`/`problems` laissés **requis** (intégrité data pour B/C/E). SCHEMA_VERSION 1→2. Rétro-compat v1 via defaults au narrowing.
- Lint `lint_standards.py` inopérant sur `/mnt/projects` (bug outil : PROJECT_ROOT hardcodé `/home/trinity`) → vérif normes manuelle OK (fichiers ≤148 l, fonctions ≤20 l, 0 any).
- **Validation extraction réelle** : conso v2 sur session `ccremote/server` → 3 problèmes extraits (dont 2 triviaux), chacun avec resolution_schema + métriques fitness peuplées, catégories génériques réutilisables. Qualité confirmée.

### Epic B — COMPLETE ✅
| Story | Status | Files | Integrity |
|---|---|---|---|
| B.1+B.2+B.5 (moteur) | ✅ | fitness.ts, champions.ts, types.ts, tests/champions.test.ts | test 30/30 |
| B.3+B.4 (intégration) | ✅ | store.ts, run.ts, api.ts | tsc 0 |
- Fitness composite : `0.35·(1/turns)+0.25·(1/(bt+1))+0.20·(1/(te+1))+0.20·(q/100)` ×{resolved 1, partial 0.6, unresolved 0}. `fitnessBreakdown` exporté pour l'UI.
- Bug intégration corrigé en validation : résumés v1 sans `problems` → guard `?? []`. champions.json généré et validé (fitness 0.866 exact).
- Endpoints /api/champions(/:category), /api/problems.

### Epic C — COMPLETE ✅
| Story | Status | Files | Integrity |
|---|---|---|---|
| C.1+C.3 (moteur) | ✅ | evolution.ts, types.ts, tests/evolution.test.ts | test 35/35 |
| C.2 (intégration) | ✅ | store.ts, run.ts, api.ts | tsc 0 |
- Buckets hebdo ISO : recurrence_rate, avgChampionFitness, difficulty, tendances improving/worsening/flat. Métrique maîtresse opérationnelle.
- Bug v1 corrigé : guard `s.difficulty?.level`. evolution.json validé.

### Epic D — COMPLETE ✅
| Story | Status | Files | Integrity |
|---|---|---|---|
| D.1+D.2 (purs) | ✅ | champion-context.ts, classifier.ts, tests/injection.test.ts | test 45/45 |
| D.5 (trace) | ✅ | types.ts, store.ts, api.ts, tests/injections-store.test.ts | tsc 0 |
| D.3+D.4 (hooks) | ✅ | hooks/{hook-io,session-start,user-prompt-submit}.ts, install-hooks.sh | E2E manuel OK |
- Hooks fail-safe (try/catch total, `{}` exit 0 sur toute erreur), anti-récursion ARCADE_LOOP_ACTIVE=1.
- **install-hooks.sh GATÉ** : non exécuté, ~/.claude/settings.json INCHANGÉ. Active l'injection sur TOUTES les sessions Claude Code → go explicite de Chris requis (comme le timer systemd).
- Validé manuellement : T1 SessionStart injecte 3 champions du projet, T2 UserPromptSubmit classifie le prompt → 2 catégories, T3 loop → `{}`.

### Epic E — COMPLETE ✅
| Story | Status | Files | Integrity |
|---|---|---|---|
| Nav + 5 panneaux | ✅ | App.tsx, Sidebar.tsx, lib/format.tsx, {Sessions,Problems,Schemas,Evolution,Injections}Panel.tsx | tsc 0, test 45/45 |
- Nav restructurée en liste verticale 2 groupes (Arcade / Apprentissage).
- **Validation E2E Playwright (5 onglets, screenshots analysés, 0 page-error, 0 erreur backend)** :
  - Sessions : difficulté+pourquoi, liste exhaustive des problèmes, resolution_schema complet (steps numérotés, outils, métriques, outcome), tags v1.
  - Problèmes : 3 catégories, filtres sévérité, resolvedRate.
  - Schémas : champion + breakdown fitness en 4 barres pondérées + multiplicateur + total (0.866 exact).
  - Évolution : 2 cartes-signaux (réapparition « doit baisser » / fitness « doit monter ») + note historique.
  - Injection : boucle bout-en-bout prouvée — les 2 tests de hook tracés et affichés (event, catégories, charCount).

## Execution Summary

### Session Metrics
- Epics : 5/5 livrés. Stories : 20/20.
- Tests : 45/45 verts (7 fichiers). tsc : 0 erreur.
- Bugs trouvés en validation (rétro-compat v1) : 2, corrigés inline (guards `?? []` / `?.`).
- E2E : 5 onglets validés visuellement, 0 erreur console/backend.

### How to Run
```bash
cd /mnt/projects/claude-arcade
./start.sh   # http://localhost:4317
./stop.sh
```

### What Remains (gaté — go de Chris requis)
1. `bash src/hooks/install-hooks.sh` — active l'injection PUSH sur TOUTES les sessions Claude Code (coût tokens + modif comportement global). Non activé.
2. Backfill complet des ~557 sessions en conso v2 — densifie champions/évolution (les tendances et comparaisons s'activent à 2+ occurrences/buckets). Coût tokens cloud Sonnet.

